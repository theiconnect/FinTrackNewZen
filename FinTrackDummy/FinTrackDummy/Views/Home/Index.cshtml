@* @{
    Layout = "~/Views/Shared/_LoginLayout.cshtml";
}
 *@
@{
    Layout = null;
}
<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <title>Login</title>
    <link rel="stylesheet" href="~/css/login.css" />
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet" />
</head>
<body>
    <div class="overall-container">
        <h2 class="main-header text-center mb-4">FinTrackNewZen</h2>
        <div class="content-container">
            <div class="logo-panel">
                <img src="~/images/LogoFinTrackNewZen.jpeg" alt="FinTrackNewZen Logo" />
            </div>
            <div class="form-panel">
                <h4 class="text-center mb-4">Login</h4>

                <form id="loginForm">
                    <div class="mb-3">
                        <label for="emailPhone" class="form-label">Email/Phone</label>
                        <input type="text" id="emailPhone" class="form-control" placeholder="Email or Phone" required />
                    </div>
                    <div class="mb-3">
                        <label for="password" class="form-label">password</label>
                        <input type="password" id="password" class="form-control" placeholder="Password" required />
                    </div>

                    @* <div class="mb-3">
                        @Html.LabelFor(model => model.EmailOrPhone, new { @class = "form-label" })
                        @Html.TextBoxFor(model => model.EmailOrPhone, new { @class = "form-control", placeholder = "Email or Phone", required = "required" })
                    </div>
                    <div class="mb-3">
                        @Html.LabelFor(model => model.Password, new { @class = "form-label" })
                        @Html.PasswordFor(model => model.Password, new { @class = "form-control", placeholder = "Password", required = "required" })
                    </div> *@
                    <p class="text-center">
                        <a href="~/Account/ForgotPassword">Forgot Password?</a>
                    </p>
                    <div class="d-grid mb-2">
                        <button type="submit" class="btn btn-success">Login</button>
                    </div>
                </form>
            </div>
        </div>
    </div>

    <script src="~/js/login.js"></script>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
